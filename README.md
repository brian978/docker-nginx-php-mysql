# Basic services
* Nginx: latest
* PHP-FPM: 8.2
* MariaDB: latest
* memcached: latest

# To start up the stack you need to run
`make start`

## Connecting the PHP container with a provisioned bash
`make cli`

